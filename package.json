{
  "name": "a-world-without-builds",
  "version": "0.0.1",
  "description": "2025 talk on 'A World Without Builds'",
  "type": "module",
  "scripts": {
    "dev": "npx serve",
    "lint:js": "eslint . --fix",
    "lint:ts": "tsc --noEmit --module NodeNext examples/typescript/*.ts",
    "lint": "npm run lint:js && npm run lint:ts",
    "pretty": "prettier . --write",
    "format": "npm run lint && npm run pretty"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.30.0",
    "eslint-config-prettier": "^10.1.5",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
